# ---------------------------------------------------------------------------
# Next-Gen AI-Driven DevSecOps Automation Engine
# Version-pinned for reproducible builds. Verified on CPython 3.11-3.14.
#
#   python -m venv .venv
#   .venv/Scripts/activate            (Windows)  |  source .venv/bin/activate
#   pip install -r requirements.txt
# ---------------------------------------------------------------------------

# --- ASGI web framework ---------------------------------------------------
fastapi==0.141.1

# --- ASGI server (plain build: pure-Python wheel set, installs on 3.14) ---
#     For a Linux prod image add:  uvloop==0.21.0 ; httptools==0.6.4
uvicorn==0.52.4

# --- Data validation / serialization ------------------------------------
pydantic==2.13.5
pydantic-core==2.46.5

# --- Typed 12-factor configuration (env + .env loading) ---------------
pydantic-settings==2.15.0

# --- Server-Sent Events response (disconnect + keep-alive handling) ----
sse-starlette==3.4.11

# --- Async HTTP client: LLM provider calls + SSRF-safe GitHub fetching --
httpx==0.28.1

# --- Pinned transitive dependencies (locked for reproducibility) -------
starlette==1.6.0
anyio==4.15.1
click==8.5.0
h11==0.16.0
httpcore==1.0.9
certifi==2026.7.22
idna==3.19
python-dotenv==1.2.3
annotated-types==0.8.0
typing-extensions==4.16.0
